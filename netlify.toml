[build]
  command = "npm install && expo export -p web && cp _redirects dist/_redirects 2>/dev/null || echo '/* /index.html 200' > dist/_redirects"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
